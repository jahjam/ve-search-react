import * as Styled from './styles';

const Title = () => {
  return (
    <Styled.Title>
      <h1>
        VEsearch <Styled.TitleIcon />
      </h1>
      <span>Search for fully vegan recipes!</span>
    </Styled.Title>
  );
};

export default Title;
